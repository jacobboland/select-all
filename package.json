{
    "name": "server",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "preinstall": "npm i -g grunt-cli && npm i -g grunt-init"
    },
    "dependencies": {
    },
    "devDependencies": {
        "grunt": "0.4.0rc7",
        "grunt-cli": "~0.1.5",
        "grunt-contrib-qunit": "~0.2.0",
        "grunt-contrib-jshint": "0.3.0"
    },
    "scripts": {
        "test": "grunt test"
    }
}
